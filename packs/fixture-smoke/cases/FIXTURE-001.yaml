schemaVersion: 1
id: FIXTURE-001
title: Test user signs in to the fixture cabinet
goal: Verify a registered test user can access the read-only fixture cabinet
preconditions:
  - The user is signed out
data:
  emailFrom: QA_EMAIL
  passwordFrom: QA_PASSWORD
steps:
  - id: open-login
    instruction: Open the fixture sign-in screen
  - id: fill-credentials
    instruction: Fill the approved test credentials
  - id: submit-login
    instruction: Submit the sign-in form
oracle:
  source: product-requirement
  expect:
    - Exact text "Fixture cabinet" is visible
    - Exact text "Signed in as test user" is visible
  reject:
    - Heading "Sign in" is visible
    - Exact text "Authentication failed" is visible
safety:
  mutation: none
